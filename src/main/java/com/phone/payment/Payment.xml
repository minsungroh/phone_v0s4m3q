<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.phone.payment.PaymentMapperInter">

  <insert id="create" parameterType="PaymentVO">
     insert into payment(item, payfile1, pcnt, paymoney, contentno, mno)
     values(#{item}, #{payfile1}, #{pcnt}, #{paymoney}, #{contentno}, #{mno});
  </insert>
  
  <select id="read" resultType="PaymentVO" parameterType="int">
    select payno, item, payfile1, pcnt, paymoney, contentno, mno
    from payment
    where contentno = #{contentno}
  </select>
  
  <select id="list" resultType="PaymentVO">
    select payno, item, orderno, payfile1, pcnt, paymoney, mno
    from payment
  </select>
  
  <update id="update" parameterType="PaymentVO">
     update payment
     set pcnt=#{pcnt}
     where payno=#{payno}
  </update>
  
  <delete id="delete" parameterType="int">
    delete from payment
    where payno=#{payno}
  </delete>
</mapper>